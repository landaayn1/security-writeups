# SQL Injection в USERNAME - вход без пароля

**Платформа:** PortSwigger Web Security Academy  
**Тема:** SQL Injection (SQLi)  
**Сложность:** Apprentice (самая лёгкая)  
**Дата:** 15 августа 2026  
**Ссылка:** https://portswigger.net/web-security/sql-injection/lab-login-bypass

## Что нужно было сделать

Есть интернет-магазин с личным кабинетом. Чтобы в него войти, нужно указать логин и пароль. 

Задача: **Войти как администратор, не зная пароля.** 


## Что я поняла про уязвимость

*Есть предположение*, что, как и в SQLi в WHERE clause, сайт подставляет то, что я ввожу в форму Username и Password, непосредственно в базу данных.
Тогда получается запрос имеет вид **SELECT * FROM users WHERE username = '[ВВОД]' AND password = 'hash([ВВОД])'**
Этот факт можно проверить или опровергнуть, просто подставив кавычку в конец.

# Проверка, есть ли уязвимость
Попробуем в поле Username ввести ' и любой пароль, например, qwerty.

**Результат**: Сайт выдал ошибку 500 (Internal Server Error).

*Попробуем ещё раз.*
Теперь введём кавычку в поле Password и введём любой Username, например, admin.

**Результат**: Сайт выдал ошибку 500 (Internal Server Error).

**Вывод**: Уязвимость есть! Кавычка сломала SQL-запрос, значит форма действительно подставляет мой ввод напрямую в базу данных.

**Почему так вышло?** Так как в форму подставлено ', то запрос имеет вид:
**SELECT * FROM users WHERE username = ''' AND password = 'hash([ВВОД])'** - в первом случае
или **SELECT * FROM users WHERE username = '[ВВОД]' AND password = '''** - во втором случае

База данных не понимает, что делать с лишней кавычкой. Это синтаксическая ошибка. Она не может выполнить запрос -> возвращает ошибку серверу -> сервер показывает 500 Internal Server Error.

# Как обойти фильтр и внедрить инъекцию?

Так как пароль мы не знаем, значит нужно убрать его проверку. Самый лучший способ это сделать - просто закомментировать проверку пароля в коде.
Значок пароля в SQL - это --. Мы хотим зайти сразу под админом, поэтому в поле Username введём administrator, но с оговоркой.
Закроем кавычку, и далее поставим знак комментария, чтобы все остальное игнорировать.

*Итого*: administrator' --

И если посмотреть наш запрос, то он будет выглядеть как-то так:

**SELECT * FROM users WHERE username = 'administrator' --*' AND password = 'hash([ВВОД])'***

Часть, обозначенная курсивом проигнорируется, и мы сможем зайти под админом, не зная пароля. Можно просто ввести любой текст или ничего не вводить.

## Результат
Ввожу administrator' -- и пароль 1 - вижу успешный вход, и надпись "Congratulations, you solved the lab!"

Лабораторная работа пройдена!

## Что я узнала нового?
SQL Injection - это когда сайт подставляет ввод пользователя прямо в запрос к базе данных без проверки.

Кавычка ' - простой способ проверить, есть ли SQLi. Если сайт выдаёт ошибку - уязвимость скорее всего есть.

-- - комментарий в SQL, помогает "обрезать" ненужную часть запроса.

Такие уязвимости могут быть очень опасными: через них можно украсть все данные из базы (пароли пользователей, номера карт и т.д.).


## Как это исправить

Для защиты нужны несколько слоёв (defense in depth):

### 1. Параметризованные запросы (главная защита)
Передавать ввод пользователя отдельно от SQL-запроса, чтобы база воспринимала его как данные, а не как команду.

Плохо:
*query = "SELECT * FROM users WHERE username = '" + username + "'"*

Хорошо:
*query = "SELECT * FROM users WHERE username = ? AND password = ?"*
*cursor.execute(query, (username, password))*

Тогда ввод administrator'-- будет искаться как обычный текст,
такого пользователя не найдётся, и вход не состоится.

### 2. Валидация ввода через allowlist
Разрешить в username только безопасные символы
(буквы, цифры, подчёркивание). Всё остальное отклонять.
Это не заменяет пункт 1, но добавляет ещё один слой защиты.

### 3. Минимальные привилегии пользователя БД
Учётная запись, от которой сайт ходит в базу,
не должна иметь прав на удаление таблиц или чтение других таблиц.


## Что помогло разобраться

PortSwigger SQL Injection theory

OWASP SQL Injection Prevention Cheat Sheet

*Write-up создан в учебных целях. Автор: Maria Kozlova, 2026.*
